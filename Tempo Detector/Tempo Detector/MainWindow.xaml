<Window x:Class="Tempo_Detector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tempo-Tapper" Height="400" Width="400"
        KeyDown="Window_KeyDown"
        SizeToContent="Manual"
        ResizeMode="NoResize"
        Background="White">

    <Grid x:Name="MainGrid" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,20,0,10">
            <TextBlock Text="Tempo-Tapper" Foreground="Black" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock x:Name="BpmTextBlock" Text="BPM: 0.00" Foreground="Black" FontSize="24" HorizontalAlignment="Center"/>
        </StackPanel>

        <Button x:Name="TapButton" Grid.Row="1" Margin="20" Background="PaleVioletRed" BorderBrush="White" BorderThickness="2" Foreground="White" FontSize="36" Content="Tap" Click="TapButton_Click"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,20">
            <TextBlock Text="Volume" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Slider x:Name="VolumeSlider" Width="150" Minimum="0" Maximum="1" Value="0.5" Margin="0,0,10,0"/>
            <Button x:Name="PlayPauseButton" Content="Play" Foreground="Black" Background="White" BorderBrush="White" BorderThickness="1"/>
        </StackPanel>
    </Grid>
</Window>